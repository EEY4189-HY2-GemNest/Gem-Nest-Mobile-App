











































































































    
    
    } on FirebaseAuthException catch (e) {
      _showCustomDialog(
        title: 'Login Failed',
        message: e.message ?? 'Login failed. Please try again.',
        isError: true,
      );
    } 
    